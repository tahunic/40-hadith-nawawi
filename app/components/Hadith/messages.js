/*
 * Hadith Messages
 *
 * This contains all the text for the Hadith component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.Hadith';

export default defineMessages({
  details: {
    id: `${scope}.details`,
    defaultMessage: 'Details',
  },
});
