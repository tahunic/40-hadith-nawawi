/*
 * Search Messages
 *
 * This contains all the text for the Search container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.Search';

export default defineMessages({
  placeholder: {
    id: `${scope}.placeholder`,
    defaultMessage: 'Search',
  },
});
